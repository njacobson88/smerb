import React from 'react';
import ReactDOM from 'react-dom/client';
import './SocialScope.css';
import SocialScope from './SocialScope';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SocialScope />
  </React.StrictMode>
);
